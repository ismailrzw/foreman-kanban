apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongo-pv
  namespace: foreman
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/mongo
    type: DirectoryOrCreate

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-pvc
  namespace: foreman
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi